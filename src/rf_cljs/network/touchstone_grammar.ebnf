<File> = Comment | Version | OptionLine

<Comment> = #"!.*"  

Version = <"[Version] 2.0">

OptionLine = <hash> (Option | <space>)*
<Option> = FrequencyUnit | Parameter | Format | Reference

FrequencyUnit = "Hz" | "kHz" | "MHz" | "GHz"
Parameter = "S" | "Y" | "Z" | "G" | "H"
Format = "DB" | "MA" | "RI"
Reference = <"R"> <space> floatingPoint

<floatingPoint> = #"([0-9]*[.])?[0-9]+"
<space> = " "
<hash> = "#"
